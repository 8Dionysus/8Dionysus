# Titan Summon: Service Cohort v0

Enter AoA workspace session posture.

Spawn the service cohort explicitly:

1. Atlas as `architect`
   - Mode: read-only.
   - Task: structure, source-of-truth map, owner boundaries, route frame.

2. Sentinel as `reviewer`
   - Mode: read-only.
   - Task: drift risks, verification gates, boundary violations, review posture.

3. Mneme as `memory-keeper`
   - Mode: read-only.
   - Task: provenance, recall hygiene, seed trace, receipt candidates.

Do not spawn Forge as `coder` unless I explicitly approve mutation.

Do not spawn Delta as `evaluator` unless the route requires comparison, regression judgment, proof review, or formal verdict.

Return:

- active roster
- conditional roster not spawned
- route frame
- risk gates
- memory/provenance posture
- mutation gate status
- judgment gate status
- suggested next move

No hidden arena. No silent closure. No unapproved mutation. No proof sovereignty. No memory canonization.
