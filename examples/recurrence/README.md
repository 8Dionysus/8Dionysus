# Recurrence Rollout Examples

The canonical shared-root rollout cadence examples already live in this
repository at:

- `examples/rollout_campaign_window.example.json`
- `examples/drift_review_window.example.json`
- `examples/rollback_followthrough_window.example.json`

This directory is only a recurrence-facing bridge so the rollout bundle can
point back to the existing owner-authored windows without minting a second
source of truth.
